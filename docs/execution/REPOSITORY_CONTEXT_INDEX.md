# GeoX execution context

Active task: `GEOX_REPO_NATIVE_EXECUTION_HANDOFF_V2_ADOPTION_RECOVERY_001`.

Canonical pins:

- MIP: `Phani-Pavuluri/marketing_intelligence_platform@38f88467f55d5bc4cc64e5a58b0f08f1639a40d0`
- MMM: `Phani-Pavuluri/MMM@1b75d1d3c9f49d40f2b7ab71f524fbd2dc6d1421`

Canonical MIP V2 execution/program documents are in the pinned MIP repository's
`docs/` execution and program-control records. GeoX evidence locations include
method-family and instrument identity records under `docs/track_d/` and
`docs/track_b/`; governed-readout contracts and fixtures under
`docs/contracts/`, `docs/fixtures/`, and `tests/contracts/`; numerical-truth
evidence under `docs/05_validation/` and `tests/fixtures/`; roadmap and open
investigations under `docs/roadmap/`, `docs/investigations/`, and
`docs/track_d/`; release/readiness evidence under `docs/release/`,
`docs/validation/`, and `docs/execution/`; and deferred package-side-agent
roadmap material under `docs/architecture/` and `docs/roadmap/`. Package
behavior is under `panel_exp/`. GeoX owns design, assignment, inference,
readouts, and numerical truth. MMM owns analytical workflow artifacts; MIP owns
orchestration, decision governance, and approval boundaries.

## Fresh Chat Bootstrap

Connected GitHub is the source of truth. Perform read-only orientation after
the mandatory bootstrap and before any modification or authorization.
