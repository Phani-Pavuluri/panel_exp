# Certified calibration handoff source fixture

This GeoX-owned artifact provides deterministic, non-authorizing provenance
records for all 12 certified governed-readout fixtures. It preserves analytical
payloads, source paths, SHA-256 checksums, freshness, synthetic time scope,
method eligibility, lineage, replay, and authorization flags. It does not emit
MMM compatibility, CalibrationSignal, ExperimentEvidence, or production claims.
