{
  "name": "panel-exp-dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspace/panel_exp",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace/panel_exp,type=bind",
  "remoteUser": "vscode",
  "postCreateCommand": "python -m pip install -U pip 'poetry==1.8.5' && poetry config virtualenvs.create false && poetry install --with dev --no-interaction",
  "mounts": [
    "source=${env:SSH_AUTH_SOCK},target=/ssh-agent,type=bind"
  ],
  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  }
}
